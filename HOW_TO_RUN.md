# 🚀 วิธีรันโปรเจกต์ SmartLab Blood Cell Quality Testing

เอกสารนี้อธิบายการรันเว็บแอป Next.js (Node/TypeScript) สำหรับกระบวนการ PT:EQA แบบครบขั้นตอน โดยไม่มีการพึ่งพา Python

## วิธีที่ 1: Development (แนะนำสำหรับการพัฒนา)

Frontend (Next.js)

```
cd /Users/king_phuripol/Work/SmartLab/BloodCellQualityTesting/app
npm install
npm run dev
```

เข้าใช้งาน: http://localhost:3001

ตำแหน่งไฟล์ CSV: วางไฟล์ไว้ในโฟลเดอร์ชื่อ “Blood Test Mockup CSVs Sept 28 2025” ที่โฟลเดอร์รากของโปรเจกต์ ระบบจะอ่านไฟล์จากที่นี่อัตโนมัติในวิซาร์ด PT:EQA

ใช้งานวิซาร์ด PT:EQA: ไปที่ Dashboard → Start Wizard หรือเปิด /pt-eqa แล้วทำตามขั้นตอน Select → Validate → Evaluate → Review → Export

- หน้าสรุปวิธีการ: /pt-eqa/method (มีสรุปขั้นตอนและเกณฑ์)
- ในวิซาร์ด มีคำอธิบายแบบพับเก็บได้ (collapsible) ช่วยให้หน้าดูไม่ยาวเกินไป แต่ยังเข้าถึงรายละเอียดได้เมื่อจำเป็น

## วิธีที่ 2: Docker (Production/ทดสอบรวมระบบ)

ไฟล์ docker-compose.yml จะรันเฉพาะ frontend

```
cd /Users/king_phuripol/Work/SmartLab/BloodCellQualityTesting
docker compose up --build -d
```

- Frontend: http://localhost:3001

## โครงสร้างโปรเจกต์โดยย่อ

```
BloodCellQualityTesting/
├── app/                     # Next.js (เว็บหลักสำหรับ PT:EQA)
│   └── src/app/
│       ├── pt-eqa/         # หน้าวิซาร์ด PT:EQA
│       ├── api/pt-eqa/load # API (Node) สำหรับโหลด/ประมวลผล CSV
│       ├── dashboard/      # Dashboard และลิงก์ไปวิซาร์ด
│       └── analysis/       # หน้ารายงาน/สรุปอื่น ๆ
└── Blood Test Mockup CSVs Sept 28 2025/  # ตำแหน่ง CSV ที่ระบบอ่าน
```

## Troubleshooting (สรุป)

- ถ้า dev server เปิดไม่ได้ ให้ตรวจสอบว่า port 3001 ว่าง: lsof -i :3001
- หาก build ล้มเหลวเพราะ ESLint: เราตั้งค่า ignoreDuringBuilds ใน next.config.js ให้ build ผ่านได้ (รัน lint แยกต่างหาก)
- หากไม่พบไฟล์ CSV ให้ตรวจสอบชื่อโฟลเดอร์และสิทธิ์การอ่านไฟล์

## คำถามที่พบบ่อย

- จำเป็นต้องใช้ Python ไหม? ไม่จำเป็น ทุกอย่างรันบน Node/TypeScript
